# This configuration was generated by
# `rubocop --auto-gen-config --exclude-limit 30 --no-auto-gen-timestamp`
# using RuboCop version 1.18.0.
# The point is for the user to remove these configuration records
# one by one as the offenses are removed from the code base.
# Note that changes in the inspected code, or installation of new
# versions of RuboCop, may require this file to be generated again.

# Offense count: 6
# Configuration parameters: CheckIdentifiers, CheckConstants, CheckVariables, CheckStrings, CheckSymbols, CheckComments, CheckFilepaths, FlaggedTerms.
Naming/InclusiveLanguage:
  Exclude:
    - 'lib/rubocop_challenger/cli.rb'
    - 'spec/lib/rubocop_challenger/cli_spec.rb'
    - 'spec/lib/rubocop_challenger/go_spec.rb'
    - 'spec/lib/rubocop_challenger/pull_request_spec.rb'
